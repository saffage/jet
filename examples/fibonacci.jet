module Fibonacci

func fib(n usize) usize {
  return when n {
    0, 1 -> n
    else -> fib(n-1) + fib(n-2)
  }
}

func main() {
  $println('fib(10) = ${fib(10)}')
  $println('fib(15) = ${fib(15)}')
  $println('fib(20) = ${fib(20)}')
}
